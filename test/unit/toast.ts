import { } from 'mocha'; /* it, describe, context, before, beforeEach */
import { expect } from 'chai';

/* tslint:disable: no-unused-expression */

describe('Dummy', () => {
    it('should make Travis let this build pass', () => {
        expect(true).to.be.true;
    });
});